import { mkdirSync, writeFileSync } from 'node:fs';
import { dirname, join, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';
import { createDecisionEscalationTemplate, escalationSrt, validateDecisionEscalation } from '../lib/decision_escalation_template.mjs';

const root = resolve(dirname(fileURLToPath(import.meta.url)), '..');
const plan = createDecisionEscalationTemplate();
validateDecisionEscalation(plan);
const dir = join(root, 'outputs', 'factory', 'video-sequences', plan.id);
mkdirSync(dir, { recursive: true });
writeFileSync(join(dir, 'storyboard.json'), JSON.stringify(plan, null, 2));
writeFileSync(join(dir, 'captions-workprint.srt'), escalationSrt(plan));
writeFileSync(join(dir, 'production-package.json'), JSON.stringify({ id: plan.id, status: 'FORMAT_REVERSE_ENGINEERED__AWAITING_NATIVE_AUDIO_WEB_RENDER', source: plan.reference.source, storyboard: 'storyboard.json', captions: 'captions-workprint.srt', final_video: null, publish: false, render_requirement: plan.guardrails.at(-1) }, null, 2));
const serialised = JSON.stringify(plan).replaceAll('</script>', '<\\/script>');
writeFileSync(join(dir, 'review.html'), `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Decision Escalation Short</title><style>:root{--ink:#102536;--cream:#fff8ed;--teal:#72d2c0;--amber:#d39b58}*{box-sizing:border-box}body{margin:0;background:#0c1d29;color:var(--cream);font:15px/1.5 Arial,sans-serif}.w{max-width:1100px;margin:auto;padding:30px 20px 56px}h1{font-size:clamp(30px,5vw,52px);line-height:1.04;margin:8px 0 12px}.ey{color:var(--teal);font-weight:800;font-size:11px;letter-spacing:.1em}.gate{margin:20px 0;padding:15px;border:1px solid var(--amber);border-radius:12px;background:#3a2c1b}.card{background:#173548;border:1px solid #31566a;border-radius:14px;padding:18px;margin:16px 0}.sys{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.sys div{background:#102a3a;border-radius:10px;padding:12px}.sys b{display:block;color:var(--teal);font-size:20px}.scene{display:grid;grid-template-columns:70px 120px 1fr;gap:14px;padding:17px 0;border-top:1px solid #31566a}.scene:first-child{border:0}.time{color:var(--teal);font-weight:800}.phase{text-transform:uppercase;font-size:11px;letter-spacing:.08em;color:var(--amber);font-weight:800}.line{font-size:17px;font-weight:700}.detail{color:#b7c7ce;margin-top:5px;font-size:13px}.pill{display:inline-block;margin-top:9px;border:1px solid #31566a;border-radius:99px;padding:3px 8px;font-size:11px;color:#d3e5e7}@media(max-width:650px){.sys{grid-template-columns:repeat(2,1fr)}.scene{grid-template-columns:60px 1fr}.phase{grid-column:2}.line,.detail,.pill{grid-column:2}.time{grid-row:1}}</style></head><body><main class="w"><div class="ey">ORIGINAL FORMAT ADAPTATION · ${plan.id}</div><h1>Escalate the decision friction, not a patient’s fear.</h1><p>A 49-second, seven-scene original 3D scenario short. It retains the reference format’s retention mechanics but avoids copying its visual identity or using medical outcomes as stakes.</p><div class="gate"><b>Render gate:</b> native audio / verified lip-sync only. This is a storyboard and workprint—never a silent visual plus robotic TTS.</div><section class="card"><div class="sys"><div><b>49s</b>7 × 7s scenes</div><div><b>You</b>second-person narration</div><div><b>4</b>distinct decision stakes</div><div><b>28s</b>turn / clarity point</div></div><p><b>Test order:</b> hook caption → setting/palette → character treatment. Keep the body stable so retention changes can be attributed correctly.</p></section><section class="card"><h2>Scene grammar</h2><div id="scenes"></div></section><section class="card"><h2>Safety boundary</h2><ul>${plan.guardrails.map(x=>'<li>'+x+'</li>').join('')}</ul></section></main><script>const p=${serialised};document.querySelector('#scenes').innerHTML=p.scenes.map(s=>'<article class="scene"><div class="time">'+String(s.start).padStart(2,'0')+'–'+String(s.end).padStart(2,'0')+'s</div><div class="phase">'+s.phase.replaceAll('-',' ')+'</div><div><div class="line">'+s.narration+'</div><div class="detail">'+s.visual_direction+'</div><span class="pill">'+s.escalation.replaceAll('-',' ')+'</span></div></article>').join('');</script></body></html>`);
console.log(JSON.stringify({ ok: true, id: plan.id, scenes: plan.scenes.length, seconds: plan.format.seconds, output: dir }));
