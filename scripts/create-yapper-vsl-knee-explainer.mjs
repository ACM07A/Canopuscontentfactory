import { copyFileSync, existsSync, mkdirSync, writeFileSync } from 'node:fs';
import { dirname, join, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';
import { createYapperVslKneeStoryboard, storyboardMarkdown, storyboardSrt, validateYapperStoryboard } from '../lib/yapper_vsl_storyboard.mjs';

const root = resolve(dirname(fileURLToPath(import.meta.url)), '..');
const plan = createYapperVslKneeStoryboard();
validateYapperStoryboard(plan);
const out = join(root, 'outputs', 'factory', 'video-sequences', plan.id);
mkdirSync(out, { recursive: true });
const reference = join(root, 'outputs', 'factory', 'video-sequences', 'yapper-vsl-knee-visual-audition-001', 'presenter-reference.png');
if (existsSync(reference)) copyFileSync(reference, join(out, 'presenter-reference.png'));
writeFileSync(join(out, 'storyboard.json'), JSON.stringify(plan, null, 2));
writeFileSync(join(out, 'script.md'), storyboardMarkdown(plan));
writeFileSync(join(out, 'captions-workprint.srt'), storyboardSrt(plan));
writeFileSync(join(out, 'production-package.json'), JSON.stringify({
  id: plan.id,
  status: 'PREPRODUCTION_COMPLETE__RENDER_BLOCKED_FOR_SYNCED_AUDIO',
  duration_seconds: plan.format.duration_seconds,
  storyboard: 'storyboard.json', script: 'script.md', captions_workprint: 'captions-workprint.srt',
  style_panel: 'review.html', presenter_reference: existsSync(reference) ? 'presenter-reference.png' : null,
  final_video: null, publish: false,
  blocker: plan.approval_gates.audio,
  next_safe_action: 'Select and explicitly allowlist a native-audio/lip-sync video model, then generate each presenter scene from its exact narration.',
}, null, 2));

const data = JSON.stringify(plan).replaceAll('</script>', '<\\/script>');
const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>60s Explainer Production Board</title><style>
:root{--ink:#102536;--cream:#fff8ed;--mint:#72d2c0;--muted:#a8b9bf;--panel:#173548;--line:#31566a}*{box-sizing:border-box}body{margin:0;background:#0c1d29;color:var(--cream);font:15px/1.45 Inter,Arial,sans-serif}.wrap{max-width:1240px;margin:auto;padding:32px 24px 64px}h1{font-size:clamp(28px,4vw,48px);line-height:1.05;margin:8px 0 12px}h2{font-size:18px;margin:0 0 14px}.eyebrow,.pill{font-weight:800;letter-spacing:.08em;text-transform:uppercase;font-size:11px;color:var(--mint)}.lede{max-width:760px;color:#d4e1e4;font-size:18px}.gate{margin:20px 0 30px;padding:16px 18px;border:1px solid #d6a74c;background:#3c3020;border-radius:12px;color:#fff2cf}.grid{display:grid;grid-template-columns:1.1fr .9fr;gap:18px}.card{background:var(--panel);border:1px solid var(--line);border-radius:16px;padding:20px}.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.stat{padding:12px;background:#102a3a;border-radius:10px}.stat b{display:block;font-size:23px;color:var(--mint)}.avatar{width:100%;max-height:390px;object-fit:cover;object-position:center;border-radius:12px;background:#0b1720}.swatches{display:flex;gap:8px}.swatch{width:44px;height:44px;border-radius:9px;border:1px solid #fff4}.subsample{padding:14px;border-radius:12px;background:#09202d;text-align:center;font-size:24px;font-weight:900;text-transform:uppercase;text-shadow:0 0 0 #000,-2px 0 #102536,0 2px #102536,2px 0 #102536,0 -2px #102536}.subsample em{font-style:normal;color:var(--mint)}.timeline{margin-top:18px}.scene{display:grid;grid-template-columns:72px 90px 1fr 160px;gap:12px;align-items:start;padding:14px 0;border-top:1px solid var(--line)}.scene:first-child{border-top:0}.time{font-weight:800;color:var(--mint)}.type{font-size:11px;letter-spacing:.07em;text-transform:uppercase;font-weight:800;padding-top:2px}.type.broll{color:#e5c074}.type.slate{color:#c5a5ef}.line{font-size:16px;font-weight:650}.detail{font-size:13px;color:var(--muted);margin-top:5px}.method{font-size:11px;color:#d9e5e8;border:1px solid var(--line);padding:7px;border-radius:8px}.prompt{margin-top:7px;color:#9eb3bd;font-size:12px}code{white-space:pre-wrap;font-family:ui-monospace,SFMono-Regular,Consolas;font-size:12px}@media(max-width:780px){.grid{grid-template-columns:1fr}.stats{grid-template-columns:repeat(2,1fr)}.scene{grid-template-columns:62px 1fr}.method{grid-column:2}.type{grid-column:2;grid-row:1}.line{grid-column:2}.detail{grid-column:2}.prompt{grid-column:2}.scene .time{grid-row:1}.scene .type{margin-left:72px}}</style></head><body><main class="wrap"><div class="eyebrow">Production board · ${plan.id}</div><h1>60-second explainer: built as a sequence, not a five-second clip.</h1><p class="lede">A Yapper/VSL-style facilitator explainer for Reels, TikTok and Shorts. Presenter speech, captions and mouth movement must come from the same native-audio generation—not from a separate TTS overlay.</p><div class="gate"><b>Render is intentionally blocked.</b> The current approved Kling configuration produces silent visuals. This board is production-complete, but its presenter scenes must wait for an explicitly approved native-audio/lip-sync model. It is not a final reel and cannot publish.</div><section class="grid"><div class="card"><h2>Format &amp; pacing</h2><div class="stats"><div class="stat"><b>60s</b>12 × 5s beats</div><div class="stat"><b>33%</b>4 cutaways</div><div class="stat"><b>9:16</b>1080 × 1920</div><div class="stat"><b>24fps</b>creator naturalism</div></div><h2 style="margin-top:22px">Caption treatment</h2><div class="subsample">Start with <em>three questions</em>.</div><p class="detail">Inter/Arial Black · 56px · cream fill · navy 5px outline · muted-teal keyword accent · two lines maximum · lower-middle safe zone. Caption timing is regenerated from approved final audio.</p><h2 style="margin-top:22px">Audio rule</h2><p class="detail">No Pocket TTS, Windows speech, or detached voiceover. Presenter shots must be video+speech generated together, then verified against this exact script.</p></div><div class="card"><h2>Fictional facilitator educator</h2>${existsSync(reference) ? '<img class="avatar" src="presenter-reference.png" alt="Fictional presenter reference">' : '<div class="avatar"></div>'}<p class="detail">Never labelled as a doctor. No white coat, badge, credential, hospital affiliation, treatment promise, or patient identity.</p><h2 style="margin-top:20px">Palette</h2><div class="swatches"><span class="swatch" style="background:#102536"></span><span class="swatch" style="background:#fff8ed"></span><span class="swatch" style="background:#72d2c0"></span><span class="swatch" style="background:#d6a74c"></span></div></div></section><section class="card timeline"><h2>Story board &amp; exact narration</h2><div id="scenes"></div></section></main><script>const plan=${data};document.querySelector('#scenes').innerHTML=plan.scenes.map(s=>'<article class="scene"><div class="time">'+String(s.start).padStart(2,'0')+'–'+String(s.end).padStart(2,'0')+'s</div><div class="type '+s.type+'">'+s.type+'<br><span style="color:#a8b9bf">'+s.role+'</span></div><div><div class="line">'+s.narration+'</div><div class="detail">'+s.visual_direction+'</div><details class="prompt"><summary>Generation direction</summary><code>'+s.generation_prompt+'</code></details></div><div class="method">'+s.production_method.replaceAll('_',' ')+'</div></article>').join('');</script></body></html>`;
writeFileSync(join(out, 'review.html'), html);
console.log(JSON.stringify({ ok: true, id: plan.id, output: out, duration: plan.format.duration_seconds, scenes: plan.scenes.length, publish: plan.publish }));
