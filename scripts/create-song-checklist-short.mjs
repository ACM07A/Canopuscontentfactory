import { mkdirSync, writeFileSync } from 'node:fs';
import { dirname, join, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';
import { createSongChecklistTemplate, songChecklistSrt, validateSongChecklist } from '../lib/song_checklist_template.mjs';

const root = resolve(dirname(fileURLToPath(import.meta.url)), '..');
const plan = createSongChecklistTemplate();
validateSongChecklist(plan);
const dir = join(root, 'outputs', 'factory', 'video-sequences', plan.id);
mkdirSync(dir, { recursive: true });
writeFileSync(join(dir, 'storyboard.json'), JSON.stringify(plan, null, 2));
writeFileSync(join(dir, 'captions-workprint.srt'), songChecklistSrt(plan));
writeFileSync(join(dir, 'production-package.json'), JSON.stringify({ id: plan.id, status: 'FORMAT_REVERSE_ENGINEERED__AUDIO_MASTER_REQUIRED', source: plan.reference.source, storyboard: 'storyboard.json', captions: 'captions-workprint.srt', final_video: null, publish: false, render_requirement: 'Approve rights-cleared original master audio, then generate/lip-sync each A-roll scene from its exact audio slice.' }, null, 2));
const data = JSON.stringify(plan).replaceAll('</script>', '<\\/script>');
writeFileSync(join(dir, 'review.html'), `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Song Checklist Short</title><style>:root{--navy:#102536;--cream:#fff8ed;--teal:#72d2c0;--pink:#e985a6;--line:#31566a}*{box-sizing:border-box}body{margin:0;background:#0b1c28;color:var(--cream);font:15px/1.5 Arial,sans-serif}.w{max-width:1100px;margin:auto;padding:30px 20px 60px}h1{font-size:clamp(30px,5vw,50px);line-height:1.04;margin:7px 0 12px}.ey{color:var(--teal);font-size:11px;font-weight:800;letter-spacing:.1em}.gate{background:#3a2d26;border:1px solid var(--pink);padding:15px;border-radius:12px;margin:20px 0}.card{background:#173548;border:1px solid var(--line);padding:18px;border-radius:14px;margin:16px 0}.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.stats div{background:#102a3a;padding:12px;border-radius:10px}.stats b{display:block;font-size:21px;color:var(--teal)}.scene{display:grid;grid-template-columns:75px 85px 1fr 120px;gap:12px;padding:16px 0;border-top:1px solid var(--line)}.scene:first-child{border-top:0}.time{color:var(--teal);font-weight:800}.kind{text-transform:uppercase;font-size:11px;font-weight:800;letter-spacing:.08em;color:var(--pink)}.lyric{font-weight:750;font-size:17px}.detail{color:#b7c7ce;font-size:13px;margin-top:5px}.slice{font-family:monospace;color:#d9e9eb;font-size:12px;padding-top:3px}@media(max-width:680px){.stats{grid-template-columns:repeat(2,1fr)}.scene{grid-template-columns:60px 1fr}.kind{grid-column:2}.lyric,.detail{grid-column:2}.slice{grid-column:2}.time{grid-row:1}}</style></head><body><main class="w"><div class="ey">ORIGINAL FORMAT ADAPTATION · ${plan.id}</div><h1>One master track. Six locked audio slices. No lip-sync drift.</h1><p>This preserves the audio-first production logic in the reference while using an original melody, original 3D language and safe medical-travel education content.</p><div class="gate"><b>Audio is the master:</b> rights-check it, lock transcript + word timings, then feed each exact 7-second slice into its A-roll lip-sync render. This template is organic-only until platform-policy review.</div><section class="card"><div class="stats"><div><b>42s</b>six 7s beats</div><div><b>94 BPM</b>calm mnemonic</div><div><b>3 / 3</b>A-roll / B-roll</div><div><b>28s</b>clarity turn</div></div><p><b>Safe tests:</b> hook lyric, palette, setting and instrumental intensity. Never vary disclosure, evidence boundary or master beat grid during a first test.</p></section><section class="card"><h2>Beat map</h2><div id="scenes"></div></section><section class="card"><h2>Non-negotiables</h2><ul>${plan.guardrails.map(x=>'<li>'+x+'</li>').join('')}</ul></section></main><script>const p=${data};document.querySelector('#scenes').innerHTML=p.scenes.map(s=>'<article class="scene"><div class="time">'+String(s.start).padStart(2,'0')+'–'+String(s.end).padStart(2,'0')+'s</div><div class="kind">'+s.kind+'</div><div><div class="lyric">'+s.lyric+'</div><div class="detail">'+s.visual_direction+'</div></div><div class="slice">'+s.audio_slice+'</div></article>').join('');</script></body></html>`);
console.log(JSON.stringify({ ok: true, id: plan.id, seconds: plan.format.seconds, scenes: plan.scenes.length, output: dir }));
